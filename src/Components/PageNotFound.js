//A stateless component to show a page not found error upon going to the wrong URL

import React from 'react';

const PageNotFound = () => (
    <div>
        <h2> Page Not Found </h2>
    </div>
)

export default PageNotFound;